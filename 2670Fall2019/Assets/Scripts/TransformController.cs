using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TransformController : MonoBehaviour
{
    //Why there is an error? Vector3 and Time.deltaTime;?
    public FloatData speed;
    /*private void Update()
    {
        var speedVector = Vector3(speed.value, 0, 0)Time.deltaTime;
        transform.Translate(speedVector);
    }*/

    private void Update()
    {
        
    }
    
}
